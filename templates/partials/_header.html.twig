<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarColor02"
      aria-controls="navbarColor02"
      aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{ path('home.index') }}">
            Home
            <span class="visually-hidden">(current)</span>
          </a>
        </li>

        {% if app.user %}
           {% endif %}

        <li class="nav-item">
          <a class="nav-link" href="{{ path('partenaire.index') }}">
            partenaire
          </a>
        </li>

         <li class="nav-item">
          <a class="nav-link" href="{{ path('user.index') }}">
            admin
          </a>
        </li>
          <li class="nav-item">
          <a class="nav-link" href="{{ path('espace.partenaire') }}">
            Espace partenaire
          </a>
        </li>
        {# <li class="nav-item">
          <a class="nav-link" href="{{ path('recipe.index') }}">Mes recettes</a>
        </li> #}
    

      </ul>

        <div class="d-flex">
           <ul class="navbar-nav me-auto">
            {% if app.user %}
         <li class="nav-item dropdown me-5">
        
          <a class="nav-link dropdown-toggle"
            data-bs-toggle="dropdown"
            href="#"
            role="button"
            aria-haspopup="true"
            aria-expanded="false">
            {{app.user.fullname}}
          </a>
          <div class="dropdown-menu">
            {# <a class="dropdown-item" href="{{ path('user.edit',{id : app.user.id}) }}">Modifier informations</a>
            <a class="dropdown-item" href="{{ path('user.edit.password',{id : app.user.id}) }}">Modifier mot de passe</a> #}
            {# <div class="dropdown-divider"></div> #}
            <a class="dropdown-item" href="{{ path('security.logout') }}">Déconnexion</a>
            
          </div>
            
        </li>
        {% else %}
        
          <li class="nav-item">
           <a class="nav-link text-success" href="{{ path('security.login') }}">connexion</a>
        </li>
        {# <li class="nav-item">
           <a class="nav-link text-success" href="{{ path('security.registration') }}">logout</a>
        </li> #}
     
        {% endif %}
</ul>
       </div>
      
   
       
         
    
    </div>
  </div>
</nav>
